<%= form_for(current_user.project_relationships.build(projectuser_id: @project.id),
             remote: true) do |f| %>
  <div><%= f.hidden_field :projectuser_id %></div>
  <%= f.submit "Follow", class: "btn btn-medium btn-primary" %>
<% end %>